import type { KeyboardRow } from "@/components/editor";

export const keyboard: KeyboardRow[] = [
  [["—"], "Nothing to focus. A badge that filters or removes something is a button, not a badge."],
];

export const checklist = [
  "Every badge says its state in words: the colour and the dot only repeat what the text already says.",
  "Text meets 4.5:1 on its background in both themes — check any colours you change.",
  "Windows High Contrast / forced colours: badges keep a visible edge.",
  "A row of badges reads as a list, so a screen reader says how many there are.",
  "If a badge changes while the page is open, announce the change where it happens, not here.",
];
