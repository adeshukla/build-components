import type { KeyboardRow } from "@/components/editor";

export const keyboard: KeyboardRow[] = [[["Tab"], "Nothing to operate: the tiles are text, so there is nothing to tab to."]];

export const checklist = [
  "It is a description list: each label is a term and its number the description, so the pairing survives without the layout.",
  "The change is written in words (“12 more than last week”), so red and green are never the only difference.",
  "The arrow is aria-hidden decoration — it would otherwise be read as a stray character.",
  "Numbers use tabular figures so a column of them does not jitter.",
  "Nothing here is a heading level below the section heading, so the page outline stays flat.",
  "Write the real comparison. A tile with an invented percentage is worse than a tile with no change line.",
];
