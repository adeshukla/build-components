import type { KeyboardRow } from "@/components/editor";

export const keyboard: KeyboardRow[] = [
  [["—"], "Nothing to focus: these are names, not controls. Wrap one in a link only if it goes somewhere."],
];

export const checklist = [
  "Screen reader: reads the group's name, then each person's full name — never “AO”.",
  "The “+3” circle names the people it stands for, so nobody is hidden behind a number.",
  "Initials are drawn on tints that keep dark text above 4.5:1.",
  "The same name always gets the same colour, on the server and in the browser.",
  "Pictures use the person's name as their description, not “avatar” or the file name.",
];
